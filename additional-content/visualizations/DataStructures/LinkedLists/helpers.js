/* ── General Helpers ──────────────────────────────────────────── */

function toHex(n) {
    return '0x' + n.toString(16).toUpperCase().padStart(2, '0');
}

// You can add more general helpers here later (formatting, etc.)